apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: oauth2-proxy
  namespace: flux-system
spec:
  interval: 1m0s
  serviceAccountName: kustomize-controller
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./infrastructure/oauth2-proxy
  prune: true
  wait: true
  timeout: 5m
  dependsOn:
  - name: external-config-data
  - name: ecr-credentials-sync
  - name: sources
  - name: istio-base
  - name: istio-customizations
  - name: external-secrets
  postBuild:
    substituteFrom:
    - kind: ConfigMap
      name: external-config-data
