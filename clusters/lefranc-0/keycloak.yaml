apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: keycloak
  namespace: flux-system
spec:
  interval: 1m0s
  serviceAccountName: kustomize-controller
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./infrastructure/keycloak/cr
  prune: true
  wait: true
  timeout: 5m
  dependsOn:
  - name: external-config-data
  - name: ecr-credentials-sync
  - name: sources
  - name: istio-base
  - name: istio-customizations
  - name: keycloak-operator
  postBuild:
    substituteFrom:
    - kind: ConfigMap
      name: external-config-data
