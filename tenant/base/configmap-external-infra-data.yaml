apiVersion: v1
kind: ConfigMap
metadata:
  name: external-infra-data
data:
  environment: ${environment}
  tenantname: ${tenantname}
  clustername: ${clustername}
  environment: ${environment}
  vaulturl: ${vaulturl}
