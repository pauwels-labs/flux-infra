kind: AuthorizationPolicy
apiVersion: security.istio.io/v1beta1
metadata:
  name: ext-authz-keycloak-broker
  namespace: keycloak
spec:
  selector:
    matchLabels:
      istio: gateway
  action: CUSTOM
  provider:
    name: keycloak-broker
  rules:
  - to:
    - operation:
        hosts: ["echo.pauwelslabs.com"]
