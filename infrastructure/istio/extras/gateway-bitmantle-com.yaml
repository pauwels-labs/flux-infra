apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: bitmantle-com
  namespace: "${infranamespaceprefix}istio-ingress"
spec:
  selector:
    app: istio-gateway
    istio: gateway
  servers: []
